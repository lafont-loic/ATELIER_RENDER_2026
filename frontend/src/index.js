import React from 'react';
import ReactDOM from 'react-dom/client';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <h1 style={{ textAlign: 'center', marginTop: '50px' }}>
      🚀 Frontend React Déployé sur Render !
    </h1>
  </React.StrictMode>
);
